root_folder: ~/data
db_config: ~/dev/trojan-defender/experiments/db.yaml

dataset: mnist
metrics: [accuracy_score]
epochs: 12
objective_class: 0
poison_fractions: [0.05, 0.1, 0.3, 0.5]

patch:
  trials: 2
  types: ['block', 'sparse']
  proportions: [0.01, 0.05, 0.1]
  dynamic_masks: [True, False]
  dynamic_pattern: [True, False]